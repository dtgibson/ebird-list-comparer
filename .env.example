# Vercel deployment — used by GitHub Actions to deploy automatically
# Get these from: vercel.com → Account Settings → Tokens / General
# and: Project Settings → General
VERCEL_TOKEN=your-token
VERCEL_ORG_ID=your-org-id
VERCEL_PROJECT_ID=your-project-id
